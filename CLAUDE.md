# Editing the application profile (AP)

* Make edits in the original template index.bs.liquid, not its generated output index.bs.

# Commits

* For commits that touch the spec build process, including templates, diagram etc., use the `build:` commit prefix.
